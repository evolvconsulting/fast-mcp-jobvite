# THE REGISTER OF EXEMPTED DESIGN CITATIONS - the only thing that grants
# an exemption. Three columns, TAB separated:
#
#     <tracked path>  <TAB>  <start>-<end>  <TAB>  <the reason>
#
# A citation is exempt from the two wired citation gates and from
# repoint-design-citations.py if AND ONLY IF its line carries the
# REPOINT-EXEMPT marker AND the (path, address) pair appears below with a
# non-blank reason. The marker alone grants nothing - that is #142, and
# it is why 36 lines that merely NAMED the marker were silently exempt
# from two wired gates while exempting nothing.
#
# THIS FILE IS `.txt` ON PURPOSE AND MUST NOT BECOME .py, .json, .md OR
# .yaml. Every one of those suffixes IS scanned by one of the gates this
# register feeds - check-design-citations.py reads .py .toml .md .yml
# .yaml .sh, check-design-citation-shape.py reads .py .sh - so a register
# written in any of them would be scanned by the gate it governs, and
# would have to exempt itself. That self-reference IS the defect this
# file exists to close. `.txt` is in neither suffix set and is on
# scripts/check-committed-file-types.py's allowlist. Converting it is not
# a tidy-up; it reopens the hole.
#
# The key carries NO LINE NUMBER, per #6: a path:line key rots on the
# first edit above the line and then silently re-exempts whatever moved
# into the slot. The address is normalised `<start>-<end>` even when the
# citation is written as a single line.
#
# ADDING A ROW IS THE POINT OF FRICTION. It is a visible diff in a file
# whose length is reported on every run. A citation that cannot be
# defended in one sentence here does not belong in the tree.

docs/briefs/BRIEF-142-scope-the-exemption.md	373-383	R13's suggested scoped form, quoted verbatim by the brief that dispatched #142
docs/briefs/BRIEF-142-scope-the-exemption.md	99999-99999	the plant that proved R13-H1, quoted as the defect; true when written (#111)
docs/reviews/DESIGN-142-scoped-exemption.md	373-383	quotes R13's suggested form while weighing it against the register
docs/reviews/DESIGN-142-scoped-exemption.md	99999-99999	quotes the plant and the gate output naming it, as the evidence for #142
docs/reviews/REVIEW-R10.md	99999-99999	R10 planted this address to prove the bounds check fires, and quotes its own probe output as evidence
docs/reviews/check-design-citation-shape.py	311-311	a record of where a defect WAS: 311 was blank and the sentence was at 312-313
docs/reviews/check-design-citation-shape.py	373-383	one of the two ends-on-blank instances R12-N1 was raised from; a record of where the defect was
docs/reviews/check-design-citations.py	603-603	example data for the citation pattern - what it MATCHES, not a citation of anything
docs/reviews/check-design-citations.py	918-923	a record of the contracted range U1 found; repointing it would erase the evidence
docs/reviews/check-design-citations.py	918-924	example data for the citation pattern - what it MATCHES, not a citation of anything
docs/reviews/check-design-citations.py	99999-99999	the R13-H1 plant, quoted in the comment that records the defect
docs/reviews/probe-midsentence-shape.py	906-906	a record of where a defect WAS, quoted in the probe's docstring
docs/reviews/probe-repoint-fail-closed.py	100-100	an example citation this probe WRITES into a temp file; not a citation of the design
docs/worklogs/AUDIT-SURVIVORS-REPORT.md	99999-99999	quotes the planted citation as evidence of a mutation survivor
docs/worklogs/WORKLOG-115B-kind-not-path.md	99999-99999	quotes a checker failure verbatim as evidence
docs/reviews/REVIEW-R13.md	99999-99999	R13-H1's plant, quoted twice as its own evidence: the review that FOUND this defect was caught by it
docs/reviews/probe-204-orphaned-by-repoint.py	489-490	TWO MARKED LINES, AND THEY ARE NOT THE SAME KIND - R22-M1 corrected this row's first wording, which called both a reproduction of ADR-0017. :8 IS an elided reproduction of ADR-0017:16. :80 is the probe's OWN narrative sentence about what happened to that address, which is the same class as six rows already here: a record of where a citation WAS. Neither is a claim about DESIGN.md as it stands, and 489 is blank at the freeze, which is the probe's whole subject. Repointing either would falsify it, and ec57a65 forbids repointing the ADR, so no repoint is available. NOTE THE THIRD LINE: :9 reproduces ADR-0017:67 and is NOT marked or registered, because it cites in the BARE form and the gate's selector requires a filename - so this row governs what the selector can see, not every reproduction in the file.
docs/reviews/probe-218-frame-census.py	353-353	ADJUDICATED dict KEY - the address being classified, not a citation of the design. This probe resolves the plan's citations at 135c3ac and c15b138; :353 is blank at the CURRENT freeze and alive at BOTH census blobs, which is the probe working, not a stale citation. Same class as the example-data rows above. TEN MORE ADDRESSES IN THIS DICT WILL JOIN THIS ROW as the design re-freezes over them; they are not registered pre-emptively, because an exemption for something no gate has flagged records a non-finding as debt.
docs/reviews/probe-218-frame-census.py	1846-1846	ADJUDICATED dict KEY - the plan's citation as written at blob c15b138, matched by exact lookup against docs/plans/IMPLEMENTATION-PLAN.md, which is a RECORD and keeps this value; the 2026-09-08 repoint of the ADR-0036 amendment moved this key and the lookup went silent (review round 3, F8), so it is pinned here as the :353 row above is.
docs/reviews/probe-218-frame-census.py	1848-1848	ADJUDICATED dict KEY - the plan's citation as written at blob c15b138, matched by exact lookup against docs/plans/IMPLEMENTATION-PLAN.md, which is a RECORD and keeps this value; the 2026-09-08 repoint of the ADR-0036 amendment moved this key and the lookup went silent (review round 3, F8), so it is pinned here as the :353 row above is.
docs/reviews/probe-218-frame-census.py	1549-1564	ADJUDICATED dict KEY - the plan's citation as written at blob c15b138, matched by exact lookup against docs/plans/IMPLEMENTATION-PLAN.md, which is a RECORD and keeps this value; the 2026-09-08 repoint of the ADR-0036 amendment moved this key and the lookup went silent (review round 3, F8), so it is pinned here as the :353 row above is.
